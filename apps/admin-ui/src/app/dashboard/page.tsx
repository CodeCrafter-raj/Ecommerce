"use client";
import React from "react";
import { useReactTable, getCoreRowModel } from "@tanstack/react-table";
import { PieChart, Pie, Cell, ResponsiveContainer, Legend, Tooltip, BarChart, Bar } from "recharts";


const Page = () => {
    return (
        <div>
            <h1>Dashboard</h1>
        </div>
    );
};

export default Page;